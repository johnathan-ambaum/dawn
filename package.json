{
  "scripts": {
    "start": "npx tailwindcss -i css/application.css -o assets/tailwind.css --watch",
    "build": "npx tailwindcss build css/application.css -o assets/tailwind.css && theme deploy"
  },
  "dependencies": {
    "@tailwindcss/forms": "^0.5.0",
    "@tailwindcss/typography": "^0.5.2"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.4",
    "postcss-cli": "^9.1.0",
    "tailwindcss": "^3.0.23"
  }
}
